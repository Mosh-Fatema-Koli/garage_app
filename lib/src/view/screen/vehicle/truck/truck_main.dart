import 'package:flutter/material.dart';

class TruckMainPage extends StatelessWidget {
  const TruckMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
