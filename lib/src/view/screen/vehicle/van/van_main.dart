import 'package:flutter/material.dart';

class VanMainPage extends StatelessWidget {
  const VanMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
